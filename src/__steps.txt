Create a firebase project
create web application
npm i firebase
save firebase configuration and export app
Build > Authentication > Sign In method
create signup and login route.

Providers:
create a provider file
create a context and set provider.
set the children props
set the context value.
set the provider

HOSTING:
npm install -g firebase-tools (login globally for your pc)
firebase login

init firebase one time for each project
firebase init
proceed
hosting firebase (control by up & down arrow and select with space key)
go to existing project
select the project you want to deploy
which project as public directory: vite-> dist create-react-app -> build
singel page Yes
github deploy no